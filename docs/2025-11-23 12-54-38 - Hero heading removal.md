# Hero heading removal

- Removed the large H1 "Filter by Tag" in the hero; the eyebrow label now carries the section title without duplicated text.
- Context: refinement requested after multi-tag filter changes; keeps the tag panel compact while retaining the filter functionality.
