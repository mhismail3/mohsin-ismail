# Mobile header sizing

- Increased header brand font size on small screens (max-width 640px) to 1.65rem so the name reads more prominently on mobile.
- Context: mobile-only tweak to improve brand legibility while keeping desktop scale unchanged.
