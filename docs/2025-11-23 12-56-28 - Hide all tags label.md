# Hide all tags label

- Removed the "All tags" placeholder in the posts header when no filters are active; the active-tag row now stays empty unless tags are selected.
- Context: follow-up UX polish to keep the posts header clean when not filtering; previous tag filter updates remain unchanged.
